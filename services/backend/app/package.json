{
  "scripts": {
    "build": "env-cmd -e production tsc",
    "dev": "env-cmd -e development npm run dev_tsc",
    "dev_tsc": "ts-node-dev --respawn --transpile-only server.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.2",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "env-cmd": "^10.1.0",
    "express": "^4.17.1"
  }
}
